[project]
name = "gloss"
version = "0.1.0"
description = "On-device translation overlay for Windows ARM64 NPU workflows."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
gloss-text = "gloss.text.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
